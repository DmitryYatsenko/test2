import LandingScreen from "../../../components/funnel/LandingScreen";
import { LANDING_VARIANTS } from "../../../components/funnel/data";

export default function CoreLandingPage() {
  return <LandingScreen variant={LANDING_VARIANTS.core} />;
}
